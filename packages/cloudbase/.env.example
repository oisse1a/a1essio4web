# CloudBase environment used by the JavaScript SDK and CLI.
VITE_CLOUDBASE_ENV_ID=your-env-id
VITE_CLOUDBASE_REGION=ap-singapore

# Optional: use a CloudBase API key only for local SDK development.
# Never commit real credentials or expose secret keys to browser code.
VITE_CLOUDBASE_ACCESS_KEY=